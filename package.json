{
  "name": "log-platform",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}
